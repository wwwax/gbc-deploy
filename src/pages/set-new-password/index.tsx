import { SetNewPasswordForm } from '@/components/SetNewPasswordForm';
import { Hero } from '@/components/Hero';

const SetNewPassword = () => {
  return <Hero form={<SetNewPasswordForm />} />;
};

export default SetNewPassword;
